# fix — Bug 修复规划

生成 `plan/fix/<branch-name>/PLAN.md`，逐节与用户确认：

1. **Bug 描述** — 现象、触发条件、影响范围
2. **复现路径** — 最小复现步骤
3. **根因分析** — 定位到具体代码位置和原因
4. **修复方案** — 拟修改的文件和逻辑
5. **回归测试** — 需要补充哪些测试用例防止复发

收尾：提示运行 `/task` 生成修复任务清单（必须执行，**第一个任务必须是能复现 Bug 的失败测试**）
