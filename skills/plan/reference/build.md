# build — 构建系统规划

生成 `plan/build/<branch-name>/PLAN.md`，逐节与用户确认：

1. **变更内容** — 修改哪个构建工具/配置（vite/webpack/makefile 等）
2. **变更原因** — 解决什么构建问题或优化目标
3. **验证方式** — 如何确认构建正常

收尾：询问用户是否需要生成 TASK.md（见 SKILL.md 收尾规则）
